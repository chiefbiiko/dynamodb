language: minimal

os:
  - linux
  - osx

before_script:
  - curl -sL https://deno.land/x/install/install.sh | sh
  - chmod +x ./start_db.sh
  - ./start_db.sh

script:
  - ACCESS_KEY_ID=fraud SECRET_ACCESS_KEY=fraud \
    $HOME/.deno/bin/deno run --allow-read=$(pwd) --allow-env --allow-net ./test.ts